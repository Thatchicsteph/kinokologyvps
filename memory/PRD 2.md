# Kinkology — PRD

## Original Problem Statement
Create a local, self-hostable web app that acts as a bridge from the internet to control the OSSM device (KinkyMakers/OSSM-hardware). The app communicates with the device via the BLE commands it accepts, and must allow a user a certain amount of time of control.

## Architecture
- **Frontend**: React (CRA + craco), TailwindCSS, dark "hardware control deck" UI. Fonts: Chivo / JetBrains Mono / IBM Plex Sans.
- **Backend**: FastAPI + MongoDB (motor). JWT admin auth (bcrypt + PyJWT).
- **Realtime**: WebSockets. One "host" (owner's browser holding the Web Bluetooth link) + many "control" clients (guests). Backend hub relays the *active* guest's commands to the host and enforces time/queue server-side (1s ticker).
- **BLE**: Web Bluetooth API in the owner's browser (Chrome/Edge/Opera). Uses the REAL OSSM firmware v3+ protocol:
  - Service `522b443a-4f53-534d-0001-420badbabe69`
  - Command char (write) `522b443a-4f53-534d-1000-420badbabe69`
  - State char (notify) `522b443a-4f53-534d-2000-420badbabe69`
  - Commands are UTF-8 strings: `set:{speed|stroke|depth|sensation|pattern}:0-100`, `go:strokeEngine`, `go:menu`, `set:speed:0` (stop). Validated server-side with the same regex as firmware.

## User Personas
- **Owner/Admin**: runs the bridge near the device, connects BLE, issues timed access codes, monitors the live queue, can emergency-stop or skip.
- **Guest**: opens a shared link/code, waits in queue, gets timed control via a mobile-first console.

## Core Requirements (static)
1. Admin JWT login.
2. Create/manage access codes granting N minutes.
3. Guests join via code, queued, one active controller at a time, server-enforced countdown.
4. Full control console: speed, depth, stroke, sensation, 7 patterns, start/stop.
5. Live queue view + owner emergency stop / skip.
6. Web Bluetooth relay to real OSSM device.

## Implemented (2026-07-29)
- Admin auth (login/logout/me), seeded admin, bcrypt+JWT. Verified via curl.
- Access code CRUD: create, list, revoke, +minutes, delete. Verified.
- Realtime hub: queue, promotion, server-side time accounting, expiry, safety stop between turns. Verified via WS test.
- Command relay with firmware-matching validation (invalid commands dropped). Verified.
- Web Bluetooth host hook (`useBleHost`) with real UUIDs, command write, state notifications. (Requires physical device + compatible browser — cannot run in cloud/headless.)
- UI: Landing (code entry), Admin login, Admin dashboard (device host panel + owner test console + live session monitor + code management), Guest control (waiting/active/ended/invalid states). Verified via screenshots.
- **Advanced Auto Programs** (guest console): 6 automated motion routines (Wave, Build-Up, Tease/Edge, Depth Pulse, Surge, Random) that drive the device via timed commands; stop on manual touch/STOP/turn end. Frontend-driven.
- **Owner Safety Limits**: min depth floor + max speed cap set in admin dashboard, stored in `settings` collection, ENFORCED SERVER-SIDE in the relay (`Hub.clamp_command`), surfaced live in the guest console (clamped sliders + lock notes). Verified 27/27 tests (iteration_3).
- **Deployment/self-host support**: added `websockets`+`wsproto` to requirements (uvicorn WS support), `Caddyfile` for single-origin HTTPS reverse proxy.
- **Admin 2FA (TOTP)**: authenticator-app two-factor for admin login with QR enrollment (pyotp + qrcode), 10 one-time backup recovery codes (stored hashed), two-step login (password → 6-digit/recovery), enable/disable in dashboard. Enforced via short-lived `mfa_token`. Verified 37/37 tests (iteration_4).
- **Brute-force lockout / rate-limiting**: MongoDB `login_attempts` keyed by `ip:scope:email`, 5 failures → 15-min lockout (HTTP 429 + Retry-After), cleared on success. Applied to login, 2fa-login, 2fa-setup-verify, 2fa-disable. Verified 44/44 tests (iteration_5).
- **Live telemetry overlay** (`/overlay`, public, OBS-ready): backend tracks current speed/stroke/depth/sensation (post-clamp) + motion run time + session time, broadcast over public WS `/api/ws/overlay` (+ GET `/api/overlay/state`). Page renders rolling SVG sparklines + run/session timers + controller + status; `?transparent=1` for OBS. Dashboard has copy/open link card. Verified 47/47 tests (iteration_6).

## Credentials
See `/app/memory/test_credentials.md`. Preview/dev admin (env-seeded): `admin@ossm.local` / `ossm-admin-2026`.
Self-hosted Docker has NO seeded admin — owner creates it via first-run setup.

## Implemented (2026-08-22)
- **Cloudflare Calls TURN one-click**: mobile viewers on 4G/5G symmetric NAT can now finally connect. Owner pastes their `TURN_KEY_ID` + `TURN_API_TOKEN` from `dash.cloudflare.com → Realtime → TURN` into the new `CloudflareTurnCard` on `/admin` (right column below OBS Stream Setup). Backend validates against Cloudflare's `/credentials/generate-ice-servers` endpoint (HTTP 201 = OK), then Fernet-encrypts the token in `settings.cloudflare_turn` (Mongo). On every viewer session start, `GET /api/stream/ice-servers` (public) mints a fresh 1-hour credential from Cloudflare and returns the full ICE config; static Google + Cloudflare STUN are always included as fallback. Frontend `ObsStream` fetches from that endpoint before `new RTCPeerConnection(...)` so mobile Safari/Chrome get relay candidates automatically. Encryption key from `TURN_CREDENTIAL_ENCRYPTION_KEY` env (auto-generated with warning if unset — non-persistent). All endpoints login-guarded, token never returned via GET, admin actions audit-logged. New file `/app/backend/cloudflare_turn.py`. Test-hook env var `CF_TURN_ENDPOINT` overrides Cloudflare URL (used by `/app/tests/test_cloudflare_turn.py`). Docker: `docker-compose.yml` pass-through for `TURN_CREDENTIAL_ENCRYPTION_KEY` + `env.docker.example` docs Cloudflare setup path as the recommended TURN option. Verified end-to-end via `/app/tests/test_cloudflare_turn.py`: unauth→401, bad creds→400, save+status shows masked key ID, /api/stream/ice-servers includes fresh `turn:turn.cloudflare.com` candidates with per-call usernames, delete falls back to STUN only. Regression suite (WHIP media, WHIP auth, toy relay, chat + kill switch) all still pass.

## Bug fixes (2026-08-21)
- **Chat stops working after admin page refresh** — `AdminDashboard` only opened the host WebSocket while BLE or Intiface were connected (`if (ble.connected || toys.connected) ble.openHostWs()`), so a fresh page load with no toys/OSSM never connected the socket and chat send silently no-op'd. Fixed by opening `ble.openHostWs()` unconditionally on mount and closing on unmount.
- **Video cannot be seen on mobiles** — WHEP SDP answers advertised the raw container IP (`172.19.0.4`) which mobile browsers on the LAN can't reach. First iteration substituted the request's Host header into `a=candidate: ... typ host` lines, but if that Host was an FQDN (`tg30.ddns.net`) mobile Chrome/Safari silently discarded the candidates per RFC 8839 (host candidates must be IP literals or `.local` mDNS names). Final fix in `/app/backend/stream.py::_resolve_viewer_host` (async): IPv4 literals passthrough, `localhost`/`127.0.0.1`/`::1` map to `127.0.0.1`, FQDNs are resolved to IPv4 via `await loop.getaddrinfo(...)` wrapped in `asyncio.wait_for(1.5s)`, cached (60s positive TTL, 5s negative TTL, 256-entry LRU-ish cap). `rewrite_answer_candidates` detects CRLF vs LF and preserves the terminator. Also added iOS Safari autoplay hardening on the `<video>` element: explicit `muted=true` + `playsinline` set on the DOM node in `pc.ontrack`, `el.play()` called manually since Safari won't autoplay a srcObject bound after mount. Verified via testing agent iteration_15: 67/67 pytest + 4/4 regression scripts pass; FQDN → IPv4 literal, no FQDN leakage in `typ host` lines, 5 concurrent unresolvable Host lookups complete in 0.08s (proving non-blocking), event-loop ticker fires 100 times during a slowed 1s lookup.

## Implemented (2026-08-21)
- **Toys kill switch + multi-guest chat**: added two owner-facing session controls on top of the guest toys relay.
  - **Kill switch**: `hub.toys_locked` flag toggled via `POST /api/session/toys/lock` (also emits `toy:stop` to the owner's WS + broadcasts) and `POST /api/session/toys/unlock`. `handle_toy_command` short-circuits when locked so guest toy commands can't slip through. `public_state.toys.locked` now sent to guests. `ToysPanel` gained a LOCK/UNLOCK button (danger-colored, sits next to STOP TOYS) and a "GUESTS LOCKED OUT" badge in the status row. `useBleHost` accepts `onToysLock` and forwards it; `AdminDashboard` maintains local `toysLocked` state and calls `toys.stopAllToys()` immediately on lock so the owner's Intiface stops too. `GuestToys` renders a "PAUSED BY OWNER" pill and greys out the intensity slider + pattern grid whenever `snap.toys.locked` is true.
  - **Chat**: `hub.chat_msgs` ring (last 50) + per-sender `CHAT_MIN_GAP = 1s` rate limit + 250-char cap in `_append_chat`. Guest `ws_control` accepts `{type:"chat", text}` and gets `chat_history` on join. Host `ws_host` accepts owner chat and also receives `chat_history` on connect. New `DELETE /api/session/chat` clears history and broadcasts `chat_cleared` to everyone. New `ChatPanel.jsx` component used in both `AdminDashboard` (below LiveQueue in Live Session card, with clear button) and `GuestControl` (right column, on both waiting and active phases). `useBleHost` gained `onChatHistory` / `onChatMsg` / `onChatCleared` callbacks so the owner page can keep the chat state in sync with the WS.
  - **Verified** via `/app/tests/test_chat_and_lock.py`: guest gets empty `chat_history` on connect; guest→owner and owner→guest messages flow with correct `role`/`author`/`text`; rate limiter drops the 2nd+ message within 1s from the same sender; kill switch fires `toy:stop` + `toys_lock:true` to owner and `state.toys.locked` to guest; while locked, `toy:vibrate:80` from guest is dropped; unlock restores flow; `DELETE /api/session/chat` broadcasts `chat_cleared`. All three existing suites (toy relay, WHIP media, WHIP auth) still pass.
- **Guest-controlled toys (Lovense / Intiface relay)**: added `toy:*` command channel from the active guest through the backend to the owner's browser. Backend (`server.py`): new `TOY_COMMAND_RE` accepts `toy:vibrate:<0-100>`, `toy:pattern:<slug>` and `toy:stop`; `Hub.handle_toy_command` validates and relays as `{"type":"toy_command","cmd":...}` on the host WS. `public_state` exposes `toys.available/pattern/locked` to guests. Frontend: `GuestToys.jsx` (intensity slider with 60ms throttle + pattern grid + STOP). `useToys.applyRemoteCommand` parses `toy:*` strings and drives Intiface. `useToys.onStatusChange` pushes availability up to the backend via `sendHostMessage({type:"toys_status"})`. `useBleHost.onToyCommand` dispatches incoming relay to `useToys.applyRemoteCommand`. Verified via `/app/tests/test_toy_relay.py`.
- **Responsive `/c/:code` layout**: mobile stays single-column phone; desktop widens to `max-w-6xl` two-column (`lg:grid-cols-[1.4fr_1fr]` waiting / `[1.15fr_1fr]` active), stream+timer sticky. Queue number scales `text-6xl → text-8xl`. `ControlConsole` grids widen `sm:grid-cols-3 xl:grid-cols-4`.
- **OBS live video (WHIP + WHEP via aiortc)** + **Docker/NAT WebRTC support (`stream_patch.py`)** + **OBS WHIP publish token** — see earlier entries; all endpoints still working end-to-end.

## Implemented (2026-07-30)
- **Closed-loop target-HR controller** (replaces linear BPM→speed): HR Sync now takes a Target Heart Rate (BPM) and adjusts device speed via an integral controller — error = target−live BPM, speed nudged by `response`(%/s per BPM) each 1s, bounded by Min/Max Speed and rate-limited by Ramp Up/Down (%/s). Speeds up to reach the target, eases off to hold it. Safety cutoff still bypasses for instant stop; still capped by Max-Speed limit and streamed via owner_telemetry to the overlay. Presets (Gentle/Responsive/Intense) now tune response+ramp+caps. Controller simulation verified (70→hold 130 BPM: ramps to 100% then eases to ~77%); UI + presets render (screenshot).
- **Overlay HR "waiting for monitor" state**: the HR tile now shows "waiting for monitor…" when no BPM is streaming, so it's clearly present even before a monitor connects. (Backend HR→overlay path re-confirmed working in preview; if HR was missing on the user's deployment it was a stale Docker build predating the HR feature — needs `docker compose up -d --build`.)
- **Security hardening** (from external code review): (1) fixed a NameError crash in `POST /api/setup` (`setup_admin` now takes `request: Request`) — first-run setup would 500 after creating the admin; (2) CORS hardened — defaults to same-origin only, and `CORS_ORIGINS='*'` auto-disables credentials (closes reflected-origin CSRF); compose no longer hardcodes `*`; (3) auth cookies switched `SameSite=none`→`lax` (still httponly+secure); (4) `get_jwt_secret()` fails loudly if `JWT_SECRET` is missing or equals the placeholder; removed the real secret that had been shipped in `env.docker.example` (now `CHANGE_ME_run_openssl_rand_hex_32`); (5) IP-keyed brute-force lockout on public `GET /api/access/{code}` (5 invalid → 429, cleared by a valid code); (6) `parse_object_id()` → clean 404 (not 500) on malformed code ids. Verified 14/14 security pytest + frontend smoke (iteration_12); login/dashboard/CRUD/2FA-enroll/logs all regress clean. ACTION FOR USER: if they ever deployed with the old committed JWT secret, rotate `JWT_SECRET` (invalidates existing sessions).
- **One-tap HR sync presets**: Gentle / Responsive / Intense buttons in the Heart Rate Sync card set the six curve values (Resting/Peak BPM, Min/Max Speed, Ramp Up/Down) in one tap; the matching preset auto-highlights when the current config equals it. Verified: tapping Intense applied 70/140/30/100/60/60 and highlighted correctly (screenshot); build clean.
- **Configurable ramp up/down for HR sync**: HR-Sync now eases the device speed toward the BPM-derived goal instead of jumping. Two new saved settings — Ramp Up (%/s) and Ramp Down (%/s), defaults 25/50 — drive a 150ms ticker in the owner browser that steps the applied speed toward target (snaps when within one step), streaming each value via `owner_telemetry` so the overlay speed graph follows smoothly. The HR safety cutoff still bypasses the ramp for an INSTANT stop. Card shows live applied % and target. Ramp math unit-verified (0→60@25%/s≈2.4s, 100→20@50%/s≈1.65s); UI + defaults confirmed by screenshot.
- **HR Safety Cutoff (configurable)**: owner-set max BPM in Safety Limits (`hr_cutoff`, 0 = off, persisted in settings). Enforced SERVER-SIDE: when live BPM ≥ cutoff the backend force-stops the device (`set:speed:0` + `go:menu`), sets `hr_over`, and blocks all motion — `clamp_command` forces any guest `set:speed:*`/`go:strokeEngine` to `set:speed:0` until BPM recovers below cutoff-3 (hysteresis). Audit events `hr_cutoff_triggered`/`hr_cutoff_cleared`. Telemetry frame carries `hr_cutoff`+`hr_over`; overlay shows a pulsing "CUTOFF" badge and the owner HR-Sync engine also forces speed 0 while over. Verified via WS: trigger→host stop, guest speed forced to 0 while over, hysteresis recovery, persistence.
- **Sync to Heart Rate mode**: owner dashboard card (`HeartRateSync.jsx`) that maps live BPM → device speed via a configurable linear mapping (Resting BPM→Min Speed, Peak BPM→Max Speed), persisted in localStorage. While enabled it writes `set:speed:N` to the device over the owner's BLE path AND sends `{type:"owner_telemetry",speed}` over the host WS so the `/overlay` speed graph reflects it (backend handler updates `hub.telemetry.speed`). Always clamped to the owner Max-Speed safety limit. Fail-safe: auto-stops (speed 0) + disables if the HR monitor or device drops; toggle is disabled until both are connected. Verified: backend owner_telemetry→overlay speed (WS test), mapping math (incl. safety cap), and card render/gating. Live BLE-driven loop reviewed but not headless-testable (Web Bluetooth).
- **Heart-rate tracking on overlay**: owner links a Bluetooth heart-rate monitor via the standard BLE Heart Rate Service (0x180D / 0x2A37) using Web Bluetooth from the dashboard (`useHeartRate.js`). Works with chest straps/optical monitors directly and Apple Watch via a HR-broadcaster app (HeartCast/BlueHeart). Live BPM streams over authenticated WS `/api/ws/hr` → backend adds `hr_bpm`+`hr_connected` to the telemetry frame → public `/overlay` shows a BPM tile with pulsing heart + red sparkline (Sparkline gained a `max` prop). Dashboard Device Host bar has HR status pill + connect/disconnect. bpm clamped 0..300; resets on disconnect. Verified backend 6/6 + frontend live overlay update (iteration_11).
- **Audit & Activity Log**: session-level logging of BOTH security/admin events (login success/fail, account lockout, owner created, 2FA enable/disable, code create/revoke/extend/delete, limits & URL updates, emergency stop, session skip, log cleared) AND session/guest events (device connected/disconnected, guest joined queue, guest took control, turn ended + reason). No per-command logging (by design). Stored in `audit_logs` (indexed on ts desc + category) via `log_event()`. Endpoints: `GET /api/logs` (filters: category, q text-search on action/actor/target, start/end date, limit/skip + total), `DELETE /api/logs` (manual clear, keep-everything policy), `GET /api/logs/export?format=csv|json` (attachment download). Frontend: dashboard "Recent Activity" card (`RecentActivity.jsx`, auto-refresh 5s) + dedicated `/admin/logs` page (`Logs.jsx`) with filter bar, event table, CSV/JSON export, load-more pagination, and Clear All. Verified full suite 69/69 backend + 100% frontend (iteration_10).
- **Configurable Local + Global URLs (set on first login, editable later)**: first-run setup form now also collects `local_url` (owner/OBS overlay base) and `public_url` (remote-guest base). Stored in `settings` (`/api/settings` returns them; `PUT /api/settings/urls` updates). Dashboard "Base URLs" card edits them live. Guest share links build from `public_url` (fallback window.origin); overlay link builds from `local_url`. Caddy TLS domain is env-driven (`{$DOMAIN}` from `.env`). Verified full suite 59/59 backend + 100% frontend (iteration_9): guest link → `https://ChangeMe/c/<CODE>`, overlay → `http://localhost/overlay`.
- **First-run setup flow**: env-based admin seeding is now OPTIONAL (only when `ADMIN_EMAIL`+`ADMIN_PASSWORD` set — used in preview/dev). Self-hosted Docker leaves them unset, so the owner creates the admin account on first launch. Endpoints: `GET /api/setup/status`, `POST /api/setup` (min 8-char pw, 403 once set up). `AdminLogin.jsx` gates: checking → create-owner form (with URL fields) → login → 2FA. Verified via `/app/test_setup_flow.py` + `/app/backend/tests/test_setup_and_urls.py`.
- **Same-origin frontend**: `api.js` falls back to `window.location` for `WS_BASE`/`API` when `REACT_APP_BACKEND_URL` is empty (Docker/Caddy single-origin). Preview unchanged.
- **Complete Docker Compose stack** (`docker-compose.yml`): `mongo` (7.0, named volume), `backend` (FastAPI :8001), `web` (multi-stage React build → Caddy static + `/api` reverse proxy incl. WebSockets, auto HTTPS 80/443). Files: `frontend/Dockerfile`, `frontend/Caddyfile` (`{$DOMAIN}` + `http://localhost`), `frontend/.dockerignore`, `env.docker.example` (JWT_SECRET + DOMAIN), `DOCKER.md`. `restart: unless-stopped`. NOTE: Docker build/run NOT executed here (no docker daemon) — compose YAML + app code validated; user runs `docker compose up -d --build` on their Mac.

## Implemented (2026-07-29)

## Implemented (2026-07-31)
- **MuSe / Love Spouse toy compatibility (researched + documented)**: investigated adding direct support for Love Spouse-protocol vibrators (model 5390 and similar "Chinese BLE toy" hardware). Confirmed via Buttplug's own protocol-documentation issue that these toys use one-way BLE *advertisement broadcasts* (11-byte manufacturer data, company ID `0xFFF0`) rather than a connectable GATT service — so neither Web Bluetooth (browser, used for the OSSM link) nor Intiface Central can address one directly; this rules out a same-pattern in-browser bridge like `useBleHost`. Existing open-source ESP32 gateway firmwares (LS-Buttplug, LVS-Gateway) solve this by emulating a Lovense toy to Intiface on one side and broadcasting the Love Spouse packets on the other — since `useToys`/`ButtplugClient` already speaks generic Buttplug WS (not Intiface-specific), no new client code is needed once such a gateway is on the network. Documented the setup path in `ToysPanel.jsx` (in-app copy) and `README.md`; no protocol/bridge code added in this app.

## Backlog
- **P1**: Multi-device support; persistent queue across backend restarts; per-slider min/max safety caps set by owner.
- **P1**: Reconnect resilience for guest WS (auto-retry).
- **P2**: Usage history/audit log per code; scheduled/one-time codes; QR code for share links.
- **P2**: Funscript player relay (`stream:pos:time`) — firmware already supports it.

## Implemented (2026-02-01)
- **stream_patch env-loading fix (P0)**: `/app/backend/stream_patch.py` now reads `STREAM_UDP_MIN/MAX/STREAM_PUBLIC_IP` *inside* `apply()` instead of at import time. `server.py` reordered so `load_dotenv()` + `logging.basicConfig()` run BEFORE `stream_patch.apply()`, so the opt-in escape hatch documented for local/preview environments actually takes effect and its state now appears in supervisor logs. Cleaned up the null `ports:` key in `docker-compose.yml` (now fully commented). All 25 stream/patch pytests + E2E `test_patch_noop_cf_relay.py` pass.
- **Cloudflare TURN — Replace flow**: Admin card now has REPLACE and CANCEL buttons so owners can rotate CF Turn credentials without deleting the working ones first (`CloudflareTurnCard.jsx`).
- **WHIP/WHEP hardening for OBS**: added RFC 8840 `Link:` headers (multiple separate headers per WHIP RFC §4.4.4) with credentials embedded into TURN URLs (`turn:USER:PASS@host:port`), OPTIONS preflight returns the same Link headers, `PATCH /api/whip/{sid}` now actually processes trickle-ICE candidates via `pc.addIceCandidate`, and Location URLs honor `X-Forwarded-Proto` so OBS's DELETE/PATCH don't get 308-redirected. All existing E2E regressions still pass.
- **KNOWN LIMITATION (2026-02-01)**: OBS 32.x on macOS (32.0.4 / 32.1.2 / 32.2.2 all tested) has a libdatachannel-based WHIP client that cannot consume ANY Link-header ICE-server format — every param name (`credential-type`, `credential`, `rel`) is misparsed as an "Invalid ICE server URL". OBS ends up gathering 0 candidates, sends a naked offer, never sends PATCH, and every ICE check times out at ~60s. Server-side workaround exhausted; recommended fixes: (a) use OBS 30.2 (last libwebrtc version), or (b) use the new in-app Browser Publisher (below).

- **Browser Publisher (2026-02-01)**: New in-admin "PUBLISH FROM THIS BROWSER" card lets the owner go live over WebRTC without OBS. Picks camera+mic or screen-share, gathers ICE via the browser's native RTCPeerConnection (which handles Link-header ICE servers correctly), POSTs to `/api/whip` with the bearer token, then sets remote description from the answer. Live preview + real-time outbound bitrate + STOP button. Verified end-to-end in Playwright: status transitions IDLE → CONNECTING → LIVE, backend reports `publisher_connected: True` with audio + video tracks, and the OBS-viewer element next to it starts pulling the stream immediately (161 kbps outbound observed). `/app/frontend/src/components/BrowserPublisher.jsx`.

- **Copy Spectator Link + Guest Reactions (2026-02-02)**: two engagement features on top of the view-only spectator flow.
  - **Copy Spectator Link**: big "SHARE SPECTATOR LINK" button on `/admin` (`shareSpectatorLink` in `AdminDashboard.jsx`) — one click POSTs to `POST /api/codes/spectator-link`, which is idempotent: reuses the newest non-revoked view-only code with `label='Spectator'` (label-scoped so an owner-minted named view-only code like "Alice" is never handed out by the button) or mints one on demand, then the client copies `${public_url}/c/<CODE>` to the clipboard and toasts the code. Audit-logged as `code_created` with `source=spectator_link`.
  - **Guest Reactions**: floating one-tap emojis over the stream. New `ReactionBar.jsx` (six buttons — 🔥 💦 😩 👏 😈 💜, 400ms client throttle) shown in all three guest phases (waiting/spectator/active) AND on `/admin` under the ObsStream. Emoji click sends `{type:"reaction", emoji}` over the WS (guest control WS or owner host WS). Backend `Hub.broadcast_reaction` (server.py:319) whitelists the emoji, enforces a 400ms per-sender rate limit, tags the payload with a hex id + author label, and broadcasts to owner + every connected guest. Reactions render via new `FloatingReactions.jsx` — absolutely-positioned overlay, `pointer-events:none`, each emoji floats up 160% with a 2.4s ease-out animation (`reaction-float` keyframes in `index.css`), self-dismisses on `animationend`, and each id is remembered in a `useRef` Set so a re-sent parent array can't resurrect an already-dismissed emoji. View-only spectators can also react.
  - **Verified** — iteration_20 (feature ship) + iteration_21 (retest after de-dup fix): backend 9/9 pytest (`test_spectator_link_reactions.py` + `test_spectator_label_scoping.py`); frontend Playwright (2 contexts, clipboard perms): SHARE SPECTATOR LINK writes URL to clipboard + toast contains code; sending A, waiting 3.5s, sending B renders only B on both overlays (no resurrection); owner ReactionBar bounces to guest with author `OWNER`; a form-created view-only code with a custom label is not returned by the button.

- **Session polish set (2026-02-02, evening)**: four ergonomics upgrades on top of the reactions + spectator link work.
  - **Persistent Queue across restart**: `Hub` now tracks `active_flushed` (seconds already written to Mongo for the current turn) and `_flush_active_used_seconds()` is called from `tick()` every `FLUSH_INTERVAL_S=5` seconds. `end_active()` writes only the *unflushed* delta so seconds are never double-counted. `promote()` resets the counter on a new turn. Effect: a mid-turn backend restart no longer resets a guest to their full granted time — reconnect picks up with `remaining_seconds ≈ granted − used_seconds` (at most `FLUSH_INTERVAL_S` of slack lost). Verified end-to-end (`backend/tests/test_persistent_queue_manual.py`): 60-min code, WS open, +12s → used_seconds=10 in Mongo, `supervisorctl restart backend`, reconnect → remaining 3599→3587.
  - **Per-Emoji Throttle**: `ReactionBar.jsx` now uses a `useRef(new Map())` keyed by emoji; switching 🔥→💦 back-to-back no longer swallows the second click. Server-side `Hub.broadcast_reaction` rate-limit key upgraded from `react:<sender>` to `react:<sender>:<emoji>` so both sides align — pytest `test_per_emoji_rate_limit` added (all 8 reaction tests still green).
  - **Reaction Sound**: `FloatingReactions.jsx` synthesizes a soft 700Hz→200Hz pluck via Web Audio API on each new floating emoji. Per-viewer toggle (`data-testid='reaction-sound-toggle'`) sits top-right of the overlay with `pointer-events:auto`; state persists to `localStorage['kinkology_reaction_sound']` (default **off** so no viewer is ambushed by audio). Verified: default off, click → on, reload → on, WebAudio errors swallowed if the ctx is unavailable.
  - **QR Share popover**: New `ShareSpectatorLink.jsx` component using shadcn `Popover` + `qrcode.react`. `SHARE SPECTATOR LINK` button now opens a popover containing a scannable 192px QR (`spectator-qr`), the code (`spectator-code`), the full URL (`spectator-url`), plus `spectator-copy` and `spectator-open` buttons. Idempotent: reopening the popover reuses the same code (verified `code_a == code_b`). Old inline handler removed from `AdminDashboard.jsx`.
  - **New dep**: `qrcode.react@4.2.0` in `frontend/package.json`.


- **Room Personality set (2026-02-02, night)**: four experience upgrades focused on making the room feel like *this* room, not "a control panel".
  - **Chat Reactions**: each `chat_msg` now carries a `reactions: {emoji: [author, …]}` map. `Hub.toggle_chat_reaction(msg_id, emoji, author)` toggles membership and broadcasts a `{type:'chat_react', msg_id, reactions}` frame to owner + all guests. `ChatPanel.jsx` renders reactions as pill badges under each line (author list in `title`), hover reveals a `SmilePlus` picker (`chat-react-open-<id>`) with the same six-emoji whitelist, and clicking a badge toggles the sender's vote. Verified E2E across two contexts (admin + guest): guest sends chat, owner opens picker + picks 🔥, badge appears on both with count `1`, second click removes it.
  - **Room Themes**: owner picks one of `kink | neon | dungeon | moody` from a new `ThemePicker.jsx` card. `PUT /api/settings/theme` persists to Mongo AND fires `Hub.broadcast_theme(name)` so every currently-connected client swaps live via a `{type:'theme'}` WS frame. `ThemeSync.jsx` (mounted at App root) reads `GET /api/settings/theme` on load, honors a localStorage cache to prevent an FOUC, and applies every swap as `<html data-theme=…>` — all `--kink-*` CSS variables re-resolve to a fully coordinated palette (magenta/cyan on deep indigo, red/amber on charred black, blue/rose on midnight). Verified: admin picks Neon → immediately reskins; admin picks Dungeon → guest tab (already open) reskins within 400ms.
  - **Session Recap**: `Hub` now keeps a `session_stats[cid]` accumulator (reactions per emoji, chat_count, per-tick speed samples, granted_seconds, started_at). Bumped on chat send, reactions, and every tick while the guest is active; reset on `promote()` and dropped on `end_active()` after emitting a `{type:'session_recap', recap: {...}}` frame to the demoted client. Frontend `SessionRecap.jsx` shows a modal with TIME USED, AVG INTENSITY (peak sub), REACTIONS (top 3 emojis sub), and MESSAGES; 14s auto-dismiss with a live countdown, close X, or CTA. Verified: 1-minute code, guest joins, waits 60s, recap renders with `TIME USED 59s of 1m 0s`, `AVG INTENSITY 30%`, `REACTIONS 0`, `MESSAGES 0`.
  - **Guest Nicknames**: `NicknamePrompt.jsx` shows a modal on first WS state message when no name has been saved for the current code. On submit sends `{type:'set_nickname', name}`; server sanitizes to `[\w \-\.\!\?]` with 2-24 chars and flips `client["auto_label"]=False` so their real name shows up in chat + presence + audit logs. Choice persists to `sessionStorage.kinkology_nick_<code>` and is re-sent on every reconnect (so a WS drop doesn't reset them to "Guest"). "SKIP" writes a sentinel so returning viewers who chose anonymity aren't nagged again. Verified E2E: prompt appears, "Alex" submits, subsequent chat lines are authored `ALEX` on both sides.
  - **New backend fields**: `settings.theme` (default `kink`), `chat_msg.reactions` (default `{}`), `session_stats` Hub state (in-memory, cleared per-turn).

  - Known-minor (non-blocking): `FloatingReactions.seenRef` Set grows unbounded over a very long session; `ReactionBar` 400ms throttle is shared across all six emojis; `chat_last_sent` map never pruned for disconnected clients.


## Notes / Limitations
- Actual BLE motion control can ONLY be exercised with a real OSSM (firmware v3+) and a Web Bluetooth-capable browser (Chrome/Edge/Opera) physically near the device. Not testable in this cloud preview — protocol is implemented per firmware source.
